using UnityEngine;
using System.Collections;

public class GarageLaunchMissionMenuScript : MonoBehaviour {

	// Use this for initialization
	void OnEnable () {
		Application.LoadLevel (1);
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
